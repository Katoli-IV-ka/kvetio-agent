# Kvetio Agent — runtime-зависимости
# Установка: pip install -r requirements.txt
# Dev-инструменты (pytest, ruff, …) — в requirements-dev.txt

# HTTP
httpx>=0.27
tenacity>=8.2

# Парсинг доменов
tldextract>=5.1

# Fuzzy-matching имён компаний
rapidfuzz>=3.9

# YAML-конфиги
pyyaml>=6.0

# .env файлы
python-dotenv>=1.0

# HTML parsing
beautifulsoup4>=4.12

# Supabase
supabase>=2.4

# Notion (прямой API-доступ из notion_sync.py; MCP здесь не используется)
notion-client>=2.2

# LLM fallback для GitHub domain inference
anthropic>=0.39

# Telegram management bot (bot/)
fastapi>=0.111
uvicorn[standard]>=0.30
